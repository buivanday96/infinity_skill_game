/// Presentation category for floating battle text.
///
/// Style (color, size, weight) is resolved from this type — never passed in data.
enum DamageType {
  normal,
  critical,
  heal,
  poison,
  burn,
  miss,
  dodge,
  shield,
  mana,
  exp,
  gold,
}
